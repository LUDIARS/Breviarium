{
  "name": "@ludiars/breviarium",
  "version": "0.1.0",
  "private": true,
  "description": "Breviarium (Br): executive summary of LUDIARS project state — workflow stage, inspection grades and cached source snapshots per project.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/main.ts",
    "dev": "node src/main.ts",
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --test \"tests/**/*.test.ts\""
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
